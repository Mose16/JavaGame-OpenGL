#version 400 core

in vec2 pass_textureCoords;

out vec4 out_Colour; //Output colour of pixel

uniform sampler2D textureSampler; //IDK. Basically represents texture

void main(void) {
	
	out_Colour = texture(textureSampler, pass_textureCoords); //Returns colour of pixel on texture at those coords

}